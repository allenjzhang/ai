# Microsoft (Outlook + ToDo)
# Azure AD app registration: https://portal.azure.com
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=
# Or use client certificate / managed identity as needed

# Google Gmail
# Create OAuth 2.0 credentials: https://console.cloud.google.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth/callback

# Azure OpenAI (for digest summarization and e2e tests)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o

# Optional: token storage paths (defaults shown)
# MSAL_TOKEN_CACHE_PATH=./.cache/msal.json
# GOOGLE_TOKEN_PATH=./.cache/google-tokens.json
